# UTD Asset Manager 0.1.4-test4 测试与部署说明

本轮目标：修复 FPE 食品预览和长 `food_id` 搜索，加入独立的中文名称/介绍草稿，并交付“右键方块替换制造”的编辑协议与安全运行器。Loot JAR 与现有 KubeJS 配方、Loot 脚本均未改动。

## 已部署版本

- 客户端：`1.21.1-NeoForge_21.1.233`
- Mod：`utd_asset_manager-0.1.4-test4.jar`
- Mod SHA-256：`42E1EFD15F16B8F145A3BA78354B367BF68A04385908888CB4EAC004356B0ACD`
- 状态清单 SHA-256：`D94E529505456834C9A7913DB8CB3EFA0EB86167A022AB10C06721D9709193EC`
- 当前目录：890 个精确身份，其中 327 个来自人工标注；配方 960 条，Loot 记录 798 条。
- 运行目录内只有一个 UTD Asset Manager JAR；当前没有 `block_transforms.json`，因此没有启用方块替换规则。

旧 test3 JAR、旧状态清单、最近一次游戏导出与白名单已保存在：

`_local_snapshots/utd_asset_manager-0.1.4-test4-deploy-20260712-0948/`

## 自动验收

- Java：43 项测试通过，NeoForge Mod 构建成功。
- 网页工作台：31 项测试通过，TypeScript 与正式 Vite 构建成功。
- 真实数据网页回归：890 个身份正常载入；FPE 中文名、精确变体、名称/介绍草稿与方块规则导出均通过；浏览器控制台 0 错误。
- 导出的 FPE 方块规则保留完整 `food_id`，不携带会随食物状态变化的临时 Components。
- FPE 名称草稿固定为精确 `food_id`；同 registry 覆盖按语义目标复用并去重。
- 未完成的禁用方块草稿保留在工作台项目但不进入运行配置；未完成的启用规则会阻断导出。
- 数据校验仍报告项目原有的 1 个大配方循环；这是后续合成表审查项，不是 test4 新增回归。

## P0：基础与长搜索（约 5 分钟）

- [ ] **A1 版本**：Mods 页面显示 `UTD Asset Manager 0.1.4-test4`。
- [ ] **A2 O 键回归**：在世界、玩家背包和箱子界面分别按 `O`，均可正常打开资产界面；`Esc` 返回原界面。
- [ ] **A3 完整输入**：在项目目录搜索框完整输入或粘贴 `food_id=firstpersonfoodeating:i_bang_a`。不能在 32 字符处停止；文本超出宽度时应横向滚动。
- [ ] **A4 精确搜索**：完整搜索应只命中对应 FPE 食品。列表和检查器允许显示短形式 `food_id=i_bang_a`，但搜索、保存和导出必须保留完整值。
- [ ] **A5 旧功能**：标注、取消标注、重载与导出仍正常。

## P0：FPE 中文名和贴图（约 5 分钟）

- [ ] **F1 i_bang_a**：项目目录中的目标食品显示自己的中文名称和贴图，不再显示通用 `pack_food`、无贴图或“变”占位。
- [ ] **F2 随机抽查**：再检查至少 3 个不同 FPE 食品；中文名、贴图与 `food_id` 必须彼此对应。
- [ ] **F3 滚动稳定**：连续滚动和切换已标注/未标注 FPE，名称和贴图不能串项。
- [ ] **F4 日志**：操作后检查 `latest.log`，不应新增 `Tried to load invalid item: No key id in MapLike[{}]`。

## P0：中文名称与介绍草稿（约 5 分钟）

- [ ] **N1 编辑**：选择本机可编辑物品，点击“编辑名称 / 介绍”，填写中文名和至少两行介绍并保存。
- [ ] **N2 即时预览**：资产界面应立即显示草稿名称；悬停实际物品时，Tooltip 第一行与介绍应更新。
- [ ] **N3 持久化**：关闭并重新打开 `O` 后草稿仍在；建议重启一次游戏再次确认。
- [ ] **N4 FPE 隔离**：FPE 必须保持“当前精确身份”范围，不能把一个名称覆盖给全部 `firstpersonfoodeating:pack_food`。
- [ ] **N5 导出**：执行导出后，同时生成资产快照和 `utd-item-presentation/v1` 名称/介绍草稿。

游戏内保存的是独立草稿，资产界面与 Tooltip 可立即预览。要让名称和介绍作为正式语言资源在所有界面生效，仍需网页工作台生成语言覆盖文件并部署后重载资源。

## P0：零规则安全检查（约 3 分钟）

- [ ] **B1 普通交互**：用普通物品右键箱子、工作台、门和普通方块，原交互全部正常。
- [ ] **B2 无误扣除**：任何方块都不应被替换，背包物品也不应额外减少。

本轮只验收“没有规则时不影响游戏”。实际替换制造需在网页工作台中选择 catalyst，进入“配方 → 新建方块替换规则”，填写目标/产物并显式启用后再测。专用服务器暂时要求客户端和服务端使用相同配置；P0 规则拒绝方块实体、门/床/双格植物等多方块结构与 FakePlayer。

## “进入项目管理”的含义

它表示这个**精确物品身份**已被项目目录识别，例如来自人工标注、配方输入/产物或 Loot 策略。它不表示已经部署、同步完成、审查通过或封盘。

## 回传格式

```text
test4：PASS / FAIL
A1–A5 基础与长搜索：PASS / FAIL
F1–F4 FPE 名称贴图与日志：PASS / FAIL
N1–N5 名称介绍草稿：PASS / FAIL
B1–B2 零规则安全：PASS / FAIL
失败步骤、截图与 latest.log：
```
